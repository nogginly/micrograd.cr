name: micrograd
version: 0.2.0
license: MIT
authors:
  - nogginly <nogginly@icloud.com>

description: |
  Port of Andrej Karpathy's tiny scalar-valued autograd engine and a neural net library on top of it with PyTorch-like API

development_dependencies:

dependencies:

targets:
  ex_compat:
    main: examples/compat.cr
  ex_lecture:
    main: examples/lecture.cr
  ex_xor:
    main: examples/xor.cr
